import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ padding: '1rem', backgroundColor: '#333', color: '#fff' }}>
      <h2>RockOn Header</h2>
      {/* Teammates can add more navigation links here */}
    </header>
  );
};

export default Header;
