services:
  postgres_rockon_db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432
    volumes:
      - postgres-rock-db:/var/lib/postgresql

volumes:
  postgres-rock-db:
